# Server Configuration
NODE_ENV=development # Or production
PORT=5001

# Database
MONGO_URI=your_mongodb_connection_string_here # Replace with your actual MongoDB connection string (e.g., from MongoDB Atlas)

# JWT Security
JWT_SECRET=your_very_strong_and_secret_jwt_key_please_change # Replace with a long, random, strong secret
JWT_EXPIRES_IN=1d # Token expiry (e.g., 1d, 7d, 1h, 30m)

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:3000 # Your React App's URL

# Email Configuration (Using Nodemailer with Gmail - Adapt for other providers)
# Make sure to use App Passwords if you have 2FA enabled on Gmail
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587 # Use 465 for SSL, 587 for TLS
EMAIL_USER=your_email@gmail.com # Replace with your email
EMAIL_PASS=your_gmail_app_password # Replace with your app password or regular password (less secure)
EMAIL_FROM="Task Management System <your_email@gmail.com>" # Optional: Custom sender name/address